\ProvidesPackage{andrew_thesis}

\usepackage{microtype}

\usepackage[USenglish]{babel}

\usepackage{fontspec}
\setmainfont[Ligatures=TeX, Fractions=On]{Linux Libertine O}
\setsansfont[Ligatures=TeX]{Lato}
\setmonofont{Inconsolata}

\usepackage[style=draft,backend=biber]{biblatex}
% Labelfont=bf is just personal preference
% caption package has links target top of figure rather than caption
\usepackage[labelfont=bf]{caption}
% hyperref must be done BEFORE cleveref
\usepackage{hyperref}
\usepackage{cleveref}

% For writing/debugging -> all links set to red
\hypersetup{
  colorlinks=true,
  allcolors = {red},
}


\newcommand\inputfile[1]{%
    \InputIfFileExists{#1}{}{\typeout{No file #1.}}%
}

\linespread{1.25}


\providecommand{\eeh}{\emph{eeh}}
\providecommand{\hhe}{\emph{hhe}}

